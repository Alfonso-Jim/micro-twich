import React from "react";
import Modal from "../Modal";

const StreamDelete = () => {
  return (
    <div>
      <h3>Delete a Stream</h3>
      <Modal />
    </div>
  );
};

export default StreamDelete;
